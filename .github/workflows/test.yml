name: Test

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json
      - name: Web build (validates Vite pipeline)
        run: make web-build
      - name: Install oapi-codegen
        run: go install github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@latest
      - name: Generate API (client + server from OpenAPI spec)
        run: make generate
      - name: Setup regression tests
        run: python3 scripts/test-setup.py
      - name: Frontend tests and lint
        run: npm --prefix web test && npm --prefix web run lint
      - name: Test
        run: make test

